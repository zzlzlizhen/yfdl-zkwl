<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.remote.modules.sys.dao.SecurityDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.remote.modules.sys.entity.SecurityEntity" id="securityMap">
        <result property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="securityCode" column="security_code"/>
        <result property="type" column="type"/>
        <result property="createTime" column="create_time"/>
        <result property="email" column="email"/>
    </resultMap>
    <select id="querySecurity" resultType="com.remote.modules.sys.entity.SecurityEntity">
        select * from security where security_code=#{securityCode} and (phone = #{phone} or email = #{email})
    </select>
</mapper>