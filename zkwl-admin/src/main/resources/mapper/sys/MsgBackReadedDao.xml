<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.remote.modules.sys.dao.MsgBackReadedDao">

    <sql id="columns" >
        id as id,
        uid as uid,
        create_time as createTime,
        type as type,
        msg_back_id as msgBackId,
        device_id as deviceId,
    </sql>
<!--   <select id="queryBackIdAndUid" resultType="com.remote.modules.sys.entity.MsgBackReadedEntity">
       SELECT * FROM msg_back_readed mbr
       WHERE mbr.msg_back_id = #{msgBackId} and mbr.uid == #{uid}
   </select>-->
    <select id="queryCount" resultType="java.lang.Integer">
        select count(msg_back_id) from msg_back_readed where uid = #{uid} and type=#{type}
    </select>

</mapper>